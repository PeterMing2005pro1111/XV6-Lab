假设初始状态下桶table[i]指向的是节点A，线程1准备向它插入K1，线程2准备向它插入K2。
那么接下来线程1调用put（K1），计算出桶是i，拿到当前的头节点，也就是A。
然后线程2调用put（K2），同样落入的是桶i，拿到的头节点是A。
随后线程1创建一个新节点e1（K1），然后通过调整链表把链接变为：table[i]→e1→A,
然后线程2会进行类似的操作再把链表调整为：table[i]→e2→A，所以就会出现e1直接被覆盖丢弃了，
使得K1会在后续的get检查中发生丢失的情况